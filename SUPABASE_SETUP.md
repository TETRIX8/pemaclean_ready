# Инструкция по настройке Supabase для PemaCleaning

Для того чтобы отзывы и фотографии сохранялись в облаке, вам нужно создать проект в Supabase и правильно настроить базу данных и хранилище.

## 1. Получение ключей (URL и API Key)

1.  Зарегистрируйтесь на [Supabase.com](https://supabase.com/) и создайте новый проект.
2.  В панели управления проектом перейдите в раздел **Project Settings** (иконка шестеренки внизу слева) -> **API**.
3.  Скопируйте следующие значения:
    *   **Project URL** (выглядит как `https://xyz.supabase.co`)
    *   **Project API anon key** (длинная строка символов)
4.  Эти ключи уже вставлены в файл `reviews.js`, но если вы меняете проект, обновите их в начале файла.

## 2. Создание таблицы в базе данных

Перейдите в раздел **SQL Editor** в Supabase и выполните следующий запрос, чтобы создать таблицу для отзывов:

```sql
-- Создание таблицы отзывов
create table reviews (
  id uuid default gen_random_uuid() primary key,
  name text not null,
  rating integer not null check (rating >= 1 and rating <= 5),
  text text not null,
  photo_urls text[] default null,
  created_at timestamp with time zone default now()
);

-- Включение защиты RLS
alter table reviews enable row level security;

-- Разрешить всем читать отзывы
create policy "Allow public read access" on reviews
  for select using (true);

-- Разрешить всем добавлять отзывы
create policy "Allow public insert access" on reviews
  for insert with check (true);

-- Разрешить удаление (для админ-панели)
create policy "Allow public delete access" on reviews
  for delete using (true);
```

**Важно:** Убедитесь, что запрос выполнен успешно. Вы должны увидеть сообщение об успешном создании таблицы.

## 3. Настройка хранилища для фото (Storage)

1.  Перейдите в раздел **Storage** в панели Supabase.
2.  Нажмите **New Bucket** и создайте бакет с именем `review-photos`.
3.  **Очень важно:** Сделайте бакет **Public** (публичным), чтобы фотографии были видны на сайте.
4.  Перейдите в **Policies** для этого бакета и убедитесь, что добавлены следующие политики:
    *   **SELECT**: Разрешить всем (Public).
    *   **INSERT**: Разрешить всем (Public).
    *   **DELETE**: Разрешить всем (Public).

Если политик нет, добавьте их вручную:

```sql
-- Разрешить публичное чтение
create policy "Allow public read" on storage.objects
  for select using (bucket_id = 'review-photos');

-- Разрешить публичную загрузку
create policy "Allow public upload" on storage.objects
  for insert with check (bucket_id = 'review-photos');

-- Разрешить публичное удаление
create policy "Allow public delete" on storage.objects
  for delete using (bucket_id = 'review-photos');
```

## 4. Проверка работы

После настройки:

1.  Откройте страницу `reviews.html` в браузере.
2.  Откройте **Developer Tools** (F12) и перейдите на вкладку **Console**.
3.  Попробуйте добавить отзыв с фотографией.
4.  В консоли вы должны увидеть логи вроде:
    - `✅ Supabase инициализирован`
    - `📤 Загружаем файл: reviews/...`
    - `✅ Файл загружен`
    - `💾 Сохраняем отзыв в базу данных...`
    - `✅ Отзыв успешно сохранен`

Если видите ошибки, проверьте:
- Правильность ключей в `reviews.js`
- Наличие таблицы `reviews` в базе данных
- Наличие публичного бакета `review-photos` в Storage
- Правильность политик RLS и Storage

## 5. Решение проблем

### Отзывы не добавляются
- Проверьте консоль браузера (F12 -> Console) на наличие ошибок.
- Убедитесь, что таблица `reviews` создана в Supabase.
- Проверьте, что политика `Allow public insert access` включена для таблицы.

### Фотографии не загружаются
- Убедитесь, что бакет `review-photos` создан и **публичный**.
- Проверьте политики Storage на наличие разрешения на загрузку.
- В консоли браузера должны быть логи о загрузке файлов.

### Отзывы не показываются
- Проверьте, что политика `Allow public read access` включена.
- Убедитесь, что в таблице есть данные (проверьте в Supabase -> Table Editor -> reviews).
- Откройте консоль браузера и проверьте логи загрузки отзывов.

---

**Готово!** Теперь ваш сайт будет сохранять все данные в Supabase, и они не пропадут при очистке кэша браузера.
